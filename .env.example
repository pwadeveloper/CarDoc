# Server-side only. Never prefix secrets with VITE_.
OPENAI_API_KEY=
CARDOC_AI_MODEL=
# Optional separate model for offline page translation; must support images + structured outputs.
CARDOC_TRANSLATION_MODEL=
